<Window x:Class="Urlaubsplaner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Urlaubsplaner.ViewModel"
        mc:Ignorable="d"
        Title="Urlaubsplaner" Height="450" Width="800" d:DataContext="{d:DesignInstance Type=local:MainWindowViewModel, IsDesignTimeCreatable=True}">
    
    <Grid>
        <Border BorderBrush="Black" BorderThickness="1" Margin="10,100,10,10">
            <DataGrid ItemsSource="{Binding Applications}" Margin="0,0,0,0" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}"/>
                    <DataGridTextColumn Header="Beantragt" Binding="{Binding Added}"/>
                    <DataGridTextColumn Header="Vorname" Binding="{Binding User.FirstName}"/>
                    <DataGridTextColumn Header="Nachname" Binding="{Binding User.LastName}"/>
                    <DataGridTextColumn Header="Urlaub Anfang" Binding="{Binding From}"/>
                    <DataGridTextColumn Header="Urlaub Ende" Binding="{Binding To}"/>
                    <DataGridTextColumn Header="Dauer" Binding="{Binding Duration}"/>
                    <DataGridCheckBoxColumn Header="Genehmigt" Binding="{Binding Confirmed}"/>
                    <DataGridTextColumn Header="Genehmigt durch" Binding="{Binding ConfirmedBy}"/>
                    <DataGridTextColumn Header="Bearbeitet am" Binding="{Binding LastEdited}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        <Button Command="{Binding VacationApplicationClickCommand}" Content="Urlaubsantrag" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Height="83" Width="90"/>
    </Grid>
</Window>
